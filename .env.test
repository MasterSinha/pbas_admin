# --- DEVELOPMENT PROXY ---
# Targets the test backend URL for the local Vite proxy
VITE_BACKEND_URL=http://localhost:8002

# --- ROUTING & BUILD CONFIG ---
# Configures the build for deployment served under /panel/
VITE_APP_BASE_PATH=/panel/
VITE_ROUTER_BASENAME=/panel
VITE_API_BASE_URL=http://localhost:8002/api/v1
